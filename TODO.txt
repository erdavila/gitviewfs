- Check if repo argument is Git repository
- Branches with "/" in name
- Tags with "/" in name
- Bare repository (refs/HEAD?)
- Detached-head (refs/HEAD?)
- Repository initialized but without any commit yet (refs/HEAD? refs/commits/?)
- getattr() should always return read-only
- Signed tags
- Handle errors:
	- ls MOUNT-POINT/objects/trees
	- ls MOUNT-POINT/objects/objects
- SymbolicLink.getattr() must report correct size
- Conflicting names
	- branch and tag
	- ...
